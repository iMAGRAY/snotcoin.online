/**
 * Константы для Redis сервиса
 */

/** Время жизни кэша по умолчанию (24 часа) */
export const DEFAULT_TTL = 60 * 60 * 24;

/** Время жизни для критического кэша (72 часа) */
export const CRITICAL_TTL = 60 * 60 * 72;

/** Префиксы ключей Redis */
export const REDIS_KEY_PREFIXES = {
  /** Префикс для ключей состояния игры */
  GAME_STATE: 'game:state:',
  /** Префикс для ключей критического состояния игры */
  CRITICAL_GAME_STATE: 'game:state:critical:',
  /** Префикс для ключей метрик игры */
  METRICS: 'metrics:',
  /** Префикс для блокировок */
  LOCK: 'lock:',
  /** Префикс для временных данных */
  TEMP: 'temp:'
};

/** Таймауты для различных операций */
export const TIMEOUTS = {
  /** Таймаут для операций в миллисекундах */
  OPERATION: 5000,
  /** Таймаут для повторного подключения в миллисекундах */
  RECONNECT: 10000,
  /** Таймаут для проверки доступности в миллисекундах */
  PING: 1000,
  /** Таймаут для блокировок в миллисекундах */
  LOCK: 30000
};

/** Максимальный размер кэша в памяти (100MB) */
export const MAX_MEMORY_CACHE_SIZE = 100 * 1024 * 1024;

/** Максимальное количество попыток подключения */
export const MAX_CONNECTION_ATTEMPTS = 5;

/** Интервал очистки памяти (15 минут) */
export const MEMORY_CLEANUP_INTERVAL = 15 * 60 * 1000;

/** Интервал проверки состояния (30 секунд) */
export const HEALTH_CHECK_INTERVAL = 30 * 1000;

/** Пороги для сжатия данных */
export const COMPRESSION_THRESHOLDS = {
  /** Минимальный размер для сжатия в байтах (1KB) */
  MIN_SIZE: 1024,
  /** Оптимальный размер для сжатия в байтах (10KB) */
  OPTIMAL_SIZE: 10 * 1024,
  /** Максимальный размер данных в байтах (10MB) */
  MAX_SIZE: 10 * 1024 * 1024
}; 